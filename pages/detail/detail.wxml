<!--pages/detail/detail.wxml-->
<view class="container">
  <view class="swiper-container">
    <swiper indicator-dots indicator-color="rgba(255, 255, 255, .3)" indicator-active-color="#fff" autoplay>
      <swiper-item wx:for="{{detailData.pics}}" wx:key="{{item.pic}}">
        <image src="{{item.pic}}" id="{{item.id}}" bindtap="swiperItem" class="slide-image" />
      </swiper-item>
    </swiper>
  </view>
  <view class="btn-warp">
    <view class="btn-l">
      <l-icon type="fenxiang"></l-icon>
      <button open-type="share">1</button>
    </view>
    <view class="btn-in">
      <l-icon type="{{isShoucan?'shoucang1':'shoucang'}}" color="{{isShoucan?'#f00':''}}" bind:tap="shoucanTap"></l-icon>
    </view>
    <view class="btn-r">
      <view class="cancat-btn">联系客服</view>
    </view>
  </view>
</view>