<!--pages/list/list.wxml-->
<view>
  <l-cell wx:for="{{historyData}}" wx:key="{{index}}" label="{{item.goodsName}}" data-id="{{item.goodsId}}" access bind:tap="getDetail">
    <view slot="prepend">
      <image class="cube" src="{{item.pic}}"></image>
    </view>
  </l-cell>
</view>